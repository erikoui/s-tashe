Make chan-downlader.js add filenames and tags from the thread title to the database (metadata for each picture) - updatedb function in index.js
convert chan-downloader to a module

Make a .ejs that loads 2 random pics from the bucket
    pic links look like this:
        <process.env.COS_ENDPOINT>/<bucket name>/<filename>
    e.g:
        https://s3.eu-de.cloud-object-storage.appdomain.cloud/s-tashe-ibm-storage/1602694848775.jpg

password authentication
register page
user priviledge levels
check if file exists on cloud before uploading (also check database records and add them if missing)